# OkHttp

## 1. TCP与UDP

TCP：面向连接。 传输数据前需通过三次握手建立连接，结束后通过四次挥手断开连接。
UDP：无连接。 直接发送数据，无需预先建立或断开连接。
UDP头部开销更少仅8字节，TCP需要20~60字节，包括序列号 确认号，窗口大小等控制信息，
在此基础上，TCP能保证数据不丢失 不重复 按序到达，UDP的数据可能丢失或乱序。但UDP拥有更快的传输速度 实时性更好。在传输模式上，TCP仅支持1对1通信，UDP支持一对1 1对多 多对多。
TCP的应用场景：主要是可靠性要求较高，如网页浏览，UDP适用于实时性优先的场景：如实时视频。

## 2. TCP 三次握手与四次挥手
1. 三次握手
	1. 第一次握手，客户端发送SYN=1的报文，携带初始序列号x,客户端进入SYN_SENT状态；
	2. 第二次握手,服务端收到SYN后，发送SYN=1 和ACK=1的报文，携带自己的初始序列号y,服务端进入SYN_RCVD状态
	3. 第三次握手，客户端收到SYN+ACK后，发送ACK=1报文，确认号为y+1,序列号为x+1，客户端和服务端进入`ESTABLISHED` 状态，连接建立；
2. 三次握手的作用
	1. 防止历史重复连接，通过序列号判断是否为历史连接；
	2. 确认双向通信能力
3. 四次挥手
	1. 第一次挥手，主动关闭方如客户端发送FIN=1报文，序列号为u，客户端进入FIN_WAIT_1状态，表示不在发送数据;
	2. 第二次挥手，被动关闭方如服务端收到FIN后，发送ACK=1报文，确认号为u+1,序列号为v,服务端进入CLOSE_WAIT状态，客户端收到后进入FIN_WAIT_2状态，此时客户端到服务端的连接关闭，但服务端仍可发送数据;
	3. 第三次挥手，服务端处理完剩余数据后，发送FIN=1报文，序列号为w，确认序号仍为u+1，服务端进入LAST_ACK状态；
	4. 第四次挥手，客户端接受到FIN后，发送ACK=1的报文，确认号为w+1,序列号为u+1，客户端进入TIME_WAIT状态，等待2MSL后关闭，服务端收到ACK立即关闭
4. 四次挥手的作用
	1. 全双工关闭：每个方向需独立发送FIN和ACK，被动方可能需要时间处理剩余数据，无法立即发送FIN
	2. TIME_WAIT的作用
		1. 确保最后一个ACK到达服务端，若丢失，服务端会重传FIN；
		2. 防止旧连接的报文因网络延迟滞留干扰新连接；
5. 为什么握手是三次挥手是四次
	1. 握手时，服务端的 `SYN+ACK` 可合并为一次发送；挥手时，被动关闭方的 `ACK` 和 `FIN` 通常无法合并（需处理剩余数据）。
6. 什么是SYN洪水攻击
	1. 攻击客户端在短时间内伪造大量不存在的IP地址，向服务器不断地发送SYN包，服务器回复确认包，并等待客户的确认。由于源地址是不存在的，服务器，需要不断的重发直至超时；
	

## 3. HTTP与HTTPS

http是超文本传输协议，https是超文本安全传输协议，两者都是通过TCP与服务器建立连接，连接建立后，http直接明文传输，https会在此连接基础上增加了SSL/TLS握手，后续采用此次握手生成的会话密钥加密传输数据。
以TLS1.2握手过程为例，简化为4步
1. 客户端请求建立连接，发送支持的TLS版本，和密码套件以及随机数
2. 服务响应返回，选择的TLS版本，加密套件 随机数 和证书；
3. 客户端验证证书，并用证书中的公钥加密预主密钥返回给服务端；
4. 客户端和服务端 用前面三步中的三个随机数生成会话密钥用于后续加密通信；

## 4.  一次网络请求的发起过程

1. 输入网址，解析url,URL结构包含
   1. 协议：如http/https，浏览器会默认补全；
   2. 域名；
   3. 路径和参数
2. DNS解析：将域名转换为IP地址
   1. 首先检查缓存中是否存在该域名的Ip,缓存包括浏览器缓存、系统缓存、路由器缓存、
   2. 如无缓存则依次本地DNS服务器和跟域名服务器查询
3. 三次握手建立TCP连接，如为Https则增加TLS握手
4. 发送Http请求，请求报文结构，
   1. 包含请求方法：get/post
   2. 请求头:host
   3. 请求体：Post请求时携带表单或Json数据
5. 服务端处理请求，返回响应数据，响应报文结构包含
   1. 状态码：如请求成功为200
   2. 响应头：如表示数据类型的Content-type
   3. 响应体：HTML内容或Json数据
6. 浏览器解析HTML，渲染布局
7. 四次挥手断开连接或保持连接，复用TCP通道用于处理后续请求；

## 5. okhttp中使用的设计模式

1. 创建型模式
	1. 建造者模式：允许分步创建复杂对象，提供不同的配置，如Request，Response和OkHttpClient对象的创建
	2. 工厂方法模式：只定义创建对象的接口，由子类决定实例化哪个类。比如OkhttpClient类型实现的Call对象工厂
	3. 单例模式：确保一个类只有一个实例，并提供全局访问点。如果分发器中的线程池对象，基于双重检查同步实现的线程池对象创建
2. 结构型模式
	1. 代理模式，为其他类提供代理，以控制访问；
		1.  缓存拦截器中，`Cache` 类代理了请求的缓存逻辑，拦截请求并优先返回缓存结果
	2. 装饰者模式：动态的为对象添加额外功能；
		1. 桥接拦截器中，对原有的请求和响应添加压功能；
3. 对象的行为型模式
	1. 责任链模式:将多个处理逻辑串联成链，每个拦截器独立处理请求或响应。默认包含五个拦截器。重试与重定向、桥接、缓存、连接、发起请求；
	2. 观察者模式：定义对象间一对多的依赖，当一个对象改变时，通知所有的观察者；
		1. 定义Callback处理异步请求的回调
	3. 策略模式：定义一系列的算法，使其可以相互替换
		1. 在缓存拦截器中，根据不同的请求头来决定是否使用缓存；
4. 单例模式的实现方式
	1. 双重检查同步:
		1. 使用volitale修实例对象，避免对象创建过程的发送的指令重排序 导致引用到空对象；
	2. 静态内部类
		1. JVM保证了类加载的线程安全，同时 通过访问其成员变量来控制类加载的时机实现懒加载；

## 6. 常见的状态码

1. 1开头的代表信息响应
   1. 100 Continue：客户端继续发送请求的剩余部分
2. 2开头的代表成功
   1. 200 OK：请求成功，响应中包含请求的数据
3. 3开头的代表重定向
   1. 301 永久重定向、302临时重定向：资源被移到到新的URL,客户端接下来用新的URL发起请求，浏览器会缓存永久重定向；
   2. 304：资源为修改，客户端可使用本地缓存，搭配If-Modified-Since请求使用
4. 4开头的代表客户端错误
   1. 400 ：请求语法错误或参数无效（如缺少必填字段）
   2. 403 Forbidden：服务器拒绝执行请求（如用户无权限访问资源）
   3. 404 Not Found：请求的资源不存在（如 URL 路径错误）
5. 5开头的代表服务器错误
   1. 500 Internal Server Error：服务器内部错误（如代码异常导致崩溃）。
   2. 502 Bad Gateway：作为网关或代理的服务器从上游服务器收到无效响应。
   3. 503 Service Unavailable：服务器暂时不可用（如维护或过载）
## 7. get与post的区别
1. GET一般用来请求资源，适合查询操作，POST用来提交数据，适合修改操作
2. GET参数通过URL查询字符串传递，仅支持URL格式的编码，参数暴露在地址栏中，长度受到浏览器限制，可以被浏览器缓存。
3. POST参数通过请求体传递，支持多种格式表单格式，Json格式，对用户不可见，安全性比GET高，理论上没有大小限制，不可被缓存，每次都需要提交表单。
## 8. 中间人攻击

1. 攻击
	1. 攻击者劫持通信比如DNS劫持，生成伪造的证书，冒充目标服务器；
	2. 客户端与攻击者建立加密连接，攻击者解密数据后，在与真实服务器建议一个加密连接，实现双向数据窃取。 
2. 防范
	1. 客户端要验证证书合法性，不要忽略证书警告；
	2. 采用系统内置的合法证书指纹，拒绝非指定证书；

## 9 . 双向认证

## 10. Retrofit
1. 通过动态代理生成服务接口的实例对象，
	1. 当调用接口方法时，解析方法注解和参数注解生成ServiceMethod对象，用于描述HTTP请求的细节；
2. Retrofit 本身不直接处理网络请求，请求委托给Okhttp执行，并通过CallAdapter和Converter处理数据转换和异步逻辑；
	1. GsonConverterFactory和RxJavaCallAdapterFactory
	
3. 适配器模式：将一个类的接口转换成客户端期望的另一个接口。

# Java
## JVM的内存模型
1. 程序计数器：线程私有，记录当前线程的字节码执行的行号指示器
2. Java虚拟机栈：线程私有，每个方法调用对应一个栈帧，栈帧中包含
	1. 局部变量表：~~存储方法参数和局部变量~~
	2. 操作数栈：~~存储计算过程中的临时操作数，是字节码指令的工作区。~~
	3. 动态链接：指向运行时常量池中该方法的符号引用，用于支持多态；
	4. 方法的返回地址：~~记录方法正常退出或异常退出的位置~~。
3. 本地方法栈：与虚拟机栈类似，它为本地方法服务；
4. Java堆：线程共享，几乎所有的对象都在堆上分配，是垃圾回收的主要区域；
5. 方法区：线程共享，存储类的元数据，静态变量和运行时常量池以及即时编译后的代码；
6. 运行时常量池：方法区的一部分，当类被加载后，存储编译期生成的字面量和符号引用,在类加载的解析阶段，符号引用会被替换为直接引用，并支持动态添加常量，如String.intern()
	1. 字面量：
		- 字符串字面量（如 `"Hello"`）  
		- 基本类型常量（如 `final int MAX = 100;`）  
		- 其他字面量（如 `3.14F`）
	2. 直接引用：指向方法区中类、字段或方法的内存地址或偏移量。
	3. 符号引用：存储在class文件的常量池中，如类名、方法名、字段名等
## 垃圾回收算法
### 判断对象是否可回收
JVM采用可达性分析算法，从GCroot出发，不可达的对象的可回收
### GCRoots
1. 虚拟机栈中的局部变量
2. 方法区中的静态变量和常量
3. JNI全局引用
4. 活跃的线程对象：线程本身以及其执行栈中的对象；
5. 被同步锁持有的对象；
### 回收算法的类型
1. 标记-清除算法：遍历堆内存，回收未被标记的对象
	1. 缺点：效率低，遍历2次，还会产生内存碎片;
2. 复制算法：将内存分为2块区域，每次只使用其中一块，标记存活对象后，将其复制到另一块内存；
	1. 缺点：内存利用率低, 如果存活率高，复制开销大
3. 标记-整理算法：标记存活对象，将存活对象向一端移动，清理边界外的内存，消除碎片
	1. 移动对象和更新指针需要时间，会出发Stop-The-World
4. 分代收集算法
	1. 按照对象存活时间将堆分为新生代和老年代，内存占比为1:2
	2. 新生代进一步分为Eden区和Survivor区，Survivor区又分为From和To.内存占比为8：1:1
	3. 新生代对象存活率低，采用复制算法，老年代存活率高，使用标记清除或标记整理算法；
	4. 新生代空间不足，触发minorGC，老年代空间不足，触发majorGC;
	5. 新对象分配在Eden区，每经历一次Minor GC，对象年龄+1，年龄超过阈值，默认为15次，对象晋升到老年代。
## 四大引用
1.  强引用：永不回收的引用；
2. 软引用：内存不足时回收；
3. 弱引用：每次GC时会挥手；
4. 虚引用：与引用对象搭配使用，用于跟踪对象回收；
## 对象的创建过程

1. 分配内存空间
2. 初始化对象
3. 将引用指向内存空间
## 类的初始化时机
1. 创建类的对象实例
2. 访问静态字段，或静态方法
3. 反射调用，如Class.forName
4. 子类被加载时触发父类的加载；

## 类的加载过程
1. 加载：将 `.class` 文件的二进制数据加载到内存，生成 `java.lang.Class` 对象
2. 链接
	1. 验证：确保字节码合法且符合 JVM 规范
	2. 准备：为 静态变量分配内存并设置初始值（零值）
	3. 解析：将常量池中的 符号引用转换为 直接引用。  
3. 初始化：执行类的 `<clinit>()` 方法，完成静态变量赋值和静态代码块执行。
## 死锁
1. 互斥条件：资源一次只能被一个线程占用，其他线程必须等待
2. 请求和保持条件：线程在持有一个资源的同时，请求被其他线程持有的资源。
3. 不可剥夺条件：线程已获得的资源在未使用完之前不能被强制剥夺，只能由线程主动释放；
4. 循环等待条件：存在一组线程，每个线程都在等待下一个线程所持有的资源，形成环形等待；
## 线程池

## 参数介绍
1. corePoolSize：核心线程数，始终存活的线程数量，即使这些线程处于空闲状态
2. maximumPoolSize ：最大线程数，线程池允许创建的最大线程
3. keepAliveTime ：非核心线程在空闲状态下的存活时间
4. unit：线程存活时间的单位
5. workQueue： 任务队列，用于保存执行任务的阻塞队列
	1. 无界队列：LinkedBlockQueue
	2. 有界队列：ArrayBlockQueue
	3. 同步队列：不存储任务，直接将任务交给线程；
6. threadFactory ：线程创建工厂，一般用来指定线程名，优先级，守护线程属性等
7. handler：任务的拒绝策略
	1. AbortPolicy：抛出异常
	2. DiscardPolicy：静默丢弃
## 工作流程
1. 首先判断核心线程数，未超过则创建核心线程执行；
2. 尝试将任务以非阻塞的形式加入到阻塞队列，需要阻塞队列存在容量
3. 添加失败则判断最大线程数，未超过则创建普通线程执行
4. 都不满足，则执行拒绝策略
5. 任务执行完，从队列中获取剩余任务执行，如果队列为空，线程是核心线程或者处于存活时间内则阻塞等待任务
## 线程池选择
根据任务类型来选择
1. CPU密集型任务：CPU核心数+1
2. IO密集型任务：CPU核心数的2倍或更高，这个比例根据阻塞比例来定，阻塞比例=阻塞时间/总时间。比如okhttp中默认的线程池大小就是无穷大
## synchroinzed的实现原理
1. synchroinzed的实现涉及到锁升级的过程，
	1. 对象头中的MarkWord 通过2bits 标记锁的状态，分为无锁、偏向锁、轻量级锁、重量级锁；
	2. 偏向锁只记录线程ID,同一个线程访问直接验证线程ID，减少无竞争时的锁开销；
	3. 轻量级锁：其他线程尝试竞争资源时，升级为轻量级锁，创建锁记录，通过CAS操作将MarkWord替换为锁记录指针，失败则自旋，减少多线程交替执行的锁开销，也不存在实际竞争；
	4. 重量级锁：自旋超过阈值或者存在多线程竞争时，升级为重量级锁。依赖操作系统的互斥锁实现，线程竞争失败进入阻塞。
## 锁的分类
1. 乐观锁与悲观锁
	1. synchronized：假设冲突一定会发生，获取资源前先加锁
	2. 乐观锁：CAS操作，先通过原子操作修改数据，失败后重试；
2. 公平锁与非公平锁
	1. synchronized是非公平锁
	2. ReentrantLock 提供公平锁实现，等待队列不为空，才尝试获取锁
3. 独占锁与共享锁
	1. synchronized就是独占锁，只允许一个线程获得锁。
	2. 读写锁中读锁为共享锁；
## yield /wait /sleep/join
1. yield：线程从运行状态Running转为就绪状态Runnable，运行其他线程获得执行机会，但保证效果，所有线程都有机会执行，不会释放锁
2. wait：释放锁并进入等待状态，需要notify或notifyAll唤醒
3. sleep：强制线程休眠指定时间，不释放锁；
4. join：等待目标线程终止
## volatile
1. 保证可见性：线程操作变量时，通常会从主内存拷贝到线程的工作内存，volatile修饰的变量修改后立即同步到主内存；
2. 保证有序性：通过插入内存屏障，禁止指令重排序，在单例模式的双重检查锁定实现中就利用了这个特性；
3. 不保证复合操作的原子性如i++
## Hashmap的实现原理
1. 底层的数据结构为数组+链表或红黑树；
2. 数组中存放Node元素，称为桶
	1. 根据元素的hash值计算索引，hash值在计算前也会先右移16位异或。hash冲突时，同一个桶内的元素通过链表存储
	2. 当链表长度超过8而且数组长度超过64，将链表转换为红黑树，红黑树节点小于6时退化为链表；
3. 数组的长度默认为16负载因子为0.75，容量超过数组大小*负载因子进行扩容
	1. 每次按照2的倍数进行扩容，保证根据hash计算索性时不存在空间浪费（(n-1)&hash）
4. 不能保证线程安全
	1. Java8之前的头插法会有死循环产生 ;
		1. A->B, T1先迁移A，记录下一个元素B，如果下一个线程开始迁移并完成，那变成了B-A
	2. Java8之后，多线程put会有覆盖的风险；
## ConCurrentHashmap
1. 不存在hash冲突，通过cas操作修改元素，存在冲突则synchronized加锁，保证同一时刻只有一个线程操作链表或红黑树；
2. 扩容时，多线程协作，每个线程复制一部分的桶，通过ForwadingNode标记正在扩容的桶；
# Kotlin 

## 协程

### 实现原理

从协程中2个重要概念入手

1. 挂起与恢复
	1. 狭义的挂起恢复，像java中线程的join方法一样，一个线程阻塞等待另一个线程执行完再恢复执行
	2. 广义的挂起恢复，像Android的handler消息机制，子线程的执行完任务通知主线程，执行任务过程中不阻塞主线程；
	3. 协程中的挂起与恢复通过suspend函数实现，用阻塞式的代码实现非阻塞式的挂起与恢复；
	4. 挂起函数的实现原理有2个重要理论
		1. CPS转换：挂起函数经过编译后，会为方法增加一个参数，称为续体，作用与callback类似，它用来接受挂起函数的执行结果，函数执行完用来恢复挂起函数之后的逻辑。
		2. 状态机：挂起函数内部的每个挂起点都对应一个状态，同时创建该挂起函数自己的匿名续体，将状态保存在续体中，通过这个续体控制恢复挂起函数的执行，挂起前状态+1，恢复后进入下一个状态，直到函数返回。
2. 结构化并发
	
	通过层级化的作用域和父子关系来管理协程的生命周期，确保所有并发任务都能被正确完成、取消和清理，避免资源泄漏或失控任务。这里涉及到个协程中重要的类
	
	1. 协程作用域（CoroutineScope）: 定义了协程上下文环境，包括Job和协程分发器
		1. 通过lunch方法启动一个协程之后，在协程体中再启动其他协程都会是它的子Job，并且默认共享父协程的分发器。
	2. Job：表示一个协程任务，支持取消和层级管理，提供父子协程操作接口来管理父子协程。
		1. 常见的SupervisorJob,就空实现了子协程的管理接口，不处理子协程的取消和异常消息。
## Job

1. Job的生命周期
	1. New：协程已创建但未启动（例如通过 `launch(start = CoroutineStart.LAZY)`）。
	2. Active：协程正在运行（默认状态）。
	3. Completing：协程已执行完毕，但可能仍有子协程在运行。
	4. Completed：协程及其所有子协程均已完成。
	5. Cancelling：协程正在取消（例如调用了 `cancel()`）。
	6. Cancelled：协程已取消。
2. 父 Job 取消时，所有子 Job 自动取消，子 Job 的异常默认行为会传播到父 Job（可通过 `SupervisorJob` 修改）。
## Flow
1. Flow是基于协程设计的响应式编程框架，由Flow发射数据，FlowCollector收集数据；
2. Flow保留了协程上下文，保证emit和collect 这2个挂起函数都在正确的作用域中被调用；
3. Flow又分为冷流和热流
  1. 冷流：数据只有在被收集（collect）时才开始生产
  2. 热流：数据的生成与收集者无关，即使没有收集者也可以主动发射数据；常见的热流分为StateFlow和SharedFlow,
4. flowOn与subscribeOn的区别
  1. 多次subscribeOn只有第一个生效，也就是离数据源最近的生效
        1. rxjava基于装饰者实现，subscribeOn将上游被观察者的订阅放在指定线程中，当数据源在子线程中生产后，按照装饰顺序依次通知所有的观察者直到原始观察者。如果不切换回主线程，观察者也会在子线程中运行。
            2. 同理observeOn只有离观察者最近一个生效，并且影响下游的所有观察者。
  2. flowOn中每个操作符可以有自己的工作线程，挂起函数的工作线程只和上下文中的dispatcher有关，每次flowOn都会影响上游操作符；

## async与launch的区别

1. async与launch都是协程启动器，区别在于他们的返回值，launch返回Job,async返回Deferred；
	1. Deffered是Job的子类，除了可以控制协程的生命周期，还可以通过await获取协程的返回值；
	2. launch的异常会立即传播，而async的异常被封装在Deferred，只有调用await时才会抛出
2. await是一个挂起函数，Deffered是Job子类型，当await被调用时，如果当前Deffered状态已经完成，直接返回结果，如果没有完成，则调用invokeOnCompletion 将新建一个子Job加入到Deffered，用来监听Deffered的完成状态。

# Android

## View的绘制流程
View的绘制流程分为测量、布局、绘制，分别对应onMeasure,onLayout 和onDraw三个方法
1. 通过测量确定每个View的尺寸，采用递归测量，父View优先测量子View，子View的测量结果会影响父View;其中测量规格MeasureSpec包含三种模式，子View的MeasureSpec由父View和自身的布局参数共同决定；
	1. 父View是EXACTLY，子View如果是MATCH_PARENT或固定尺寸，那么子View也是EXACTLY;
	2. 父View是AT_MOST或者UNSPECIFIED,子View是固定尺寸才是EXACTLY，是其他参数则跟随父View;
2. 布局：确定View在屏幕上的位置，边界坐标由父View的分配
3. 绘制：将View的内容渲染到屏幕上，通过canvas提供的绘制方法，按照背景、自身内容、子View、绘制装饰的顺序进行绘制

## 消息机制
1. Handler消息机制涉及3个对象
	1. Looper：轮询器，轮询从消息队列中取消息，每个线程只有唯一的轮询器，
	2. MessageQueue：消息队列，保存消息，实际上的数据结构是一个单链表，节点按照消息的执行事件进行排序，同时节点会指向对应的Handler
	3. Handler：向消息队列发送消息，同时处理消息返回结果，同一个线程可以有多个Handler;
2. Looper通过ThreadLocal来保证同一个线程只初始化一次，主线程的Looper在应用启动时由系统创建，子线程需要手动创建并开启轮询；
3. MessageQueue中存在2种特殊消息，称为同步屏障和异步消息；
	1. 当Choreographer接受到V-sync信号，调用ViewRootImpl执行sheduleTraversals 绘制View时会发送这2个消息
		1. 同步屏障消息关联的Hanlder为空，异步消息的异步属性为true
	2. 消息队列在遍历到同步屏障后，优先查找队列中的异步消息执行，直到ViewRootImpl 发送消息移除屏障；

## 事件的传递机制
1. 一个事件序列由一个Down事件，多个Move事件，一个up或cancel事件组成；
2. 事件的传递顺序：依次从Activity到PhoneWindow到DecorView 到ViewGroup再到View；
3. 处理流程由3个核心方法组成：
	1. onInterceptTouchEvent：只有ViewGroup拥有，判断是否拦截事件，默认为false;
	2. dispatchTouchEvent：ViewGroup中负责分发事件，View中会直接调用onTouchEvent处理；
	3. onTouchEvent：处理事件，返回true,代表消费事件;
4. 当ViewGroup接受到Down事件后
	1. 判断是否拦截，
	2. 如果不拦截，则遍历子View，寻找处理此事件的View
		1. 如果View的onTouchEvent或者onTouchListener返回true,保存在ViewGroup中；
		2. ViewGroup通过链表保存处理Down事件的子View，对于单指触摸事件，链表中只会有一个元素；
		3. 后续的其他事件先判断是否拦截，但不会在遍历子View，而是直接使用链表中保存的子View处理点击事件；
	3. 如果一个子View，不处理Down事件，也不会接收到其他事件；
### cancel事件如何产生
1. 异常情况导致上一次事件序列还未结束，下个Down事件又到达，cancel通知子View
2. 子View接收到DOWN事件后，设置了PFLAG_CANCEL_NEXT_UP_EVENT;
3. 父View下发了Down事件，但是拦截了后续的事件；
4. View在处理事件过程中被父View移除；
### 常见的滑动冲突解决
1. 外部拦截法：父View根据条件判断是否拦截；
	1. 比如ViewPager嵌套ListView场景，在ViewPager的onIntercepetorTouchEvent方法中根据Move方向进行拦截，父View处理横向滑动，子View处理总想滑动；
2.  内部拦截法：子View通过requestDisallowInterceptTouchEvent 控制父容器是否拦截，比如ViewPagerr嵌套水平滑动的RecyclerView，重写子View的dispatchTouchEvent方法，滑动子View的边缘在运行父View拦截；
3. 这2个方法的前提，都是父View不能拦截Down事件；
## Actvity的启动模式
1. standard(默认模式)：每次启动Activity都会创建新实例，无论是否已存在
	1. 应用场景：如邮件应用的写页面；
2. singleTop(栈顶复用)：若目标Activity已位于栈顶，则复用该实例（触发onNewIntent）,否则新建实例
	1. 应用场景：通知页跳转；
3. singleTask（栈内单例）：在任务栈中仅存一个实例。启动时,若存在该Activity的实例，则清除上方所有的Acitivity,使其位于栈顶，触发onNewIntent
	1. 应用场景：应用的主页；
4. singleInstance(全局单例)：Activity独占一个任务栈，且该栈仅存此实例。其他的Activity默认放入其他栈；
	1. 应用场景：系统功能界面，如拨号盘等；
## DecorView与ViewRootImpl的关系
1. DecorView是窗口的顶级视图容器，每一个Activity或Dialog都对应一个DecorView

# Glide的缓存机制
## 三级缓存
1. 活动缓存：资源优先被活动缓存的map持有，通过引用计数和弱引用保证活动缓存的可靠性；
	1. 引用计数：资源被一个View使用，计数+1，释放-1，实现资源复用
		1. 如果没有引用计数，不知道一个资源什么时候应该回收，不能确保多个View共用一个资源
	2. 弱引用：处理Activity异常销毁或者快速滑动的View资源不能及时释放的场景
		1. 将资源与Activity的生命周期绑定，开启守护线程检测Activity是否被回收
2. Lru缓存：通过LinkHashmap实现LRU的缓存，默认容量为当前屏幕项目总数x每个像素字节数x缓存屏数，put图片时，检查空间大小，如果超过最大值，按照Lru顺序移除直到满足大小
3. 磁盘缓存：磁盘缓存分为原始资源和转换后的资源缓存，默认大小为250M，采用DiskLru缓存框架；
# Binder
1. 由于进程隔离，用户空间与用户空间要进行通信，必须要通过内核空间，客户端首先将数据拷贝到内核空间，再从内核空间拷贝到服务端；
2. 在binder通信中，服务端启动时，会先调用mmap将服务端用户空间的一端虚拟内存和内核空间的一端虚拟内存，映射到同一块物理内存上；
3. 这段物理内存作为一个缓存区，客户端发送数据时，内核会通过copy_from_user将数据放到缓存区，然后通知服务端。服务端只需要通过用户空间相应的虚拟地址，就能直接访问数据，这个就减少了一次内存拷贝；
## 常见问题
1. oneway与非oneway的区别
	1. oneway方法属于异步调用，客户端立即返回，不阻塞，方法也不允许有返回值
2. 最大线程数
	1. 主线程+15个子线程；
# ANR
1. ANR的类型：
	1. 输入事件超时：应用在5秒内无法处理触摸、按键等输入事件；
	2. 广播接收超时：前台广播10s，后台广播60s未完成onReceiver方法；
	3. 服务启动超时：前台Service在20s内、后台Service在200s内未启动完成；
	4. 内容提供者发布者发布超时：10s内没有完成发布流程；
2. ANR分析流程
	1. anr相关的日志有3种，mainLog也就是应用日志、 event日志和anr trace日志，首先对比3份日志，找到anr的起点事件，anr原因，确定日志的有效性，线上的anr日志经常会丢失日志；
	2. 通过trace日志，检查主线程的状态
		1. main线程处于BLOCK、WAITING、TIMEWATING状态，那基本上是函数阻塞导致的ANR；
			1. BLOCK为阻塞等锁状态
			2. WAITING为等待唤醒如调用了wait方法
			3. TIMEWATING为指定时间的休眠如sleep
		2. main线程处于NATIVE状态,同时Looper.pollInner是表示当前Looper正常，没有阻塞，正在等待消息执行；
		3. 如果main线程无异常，则通过mainlog排查CPU负载和内存环境
	3. mainlog中会输出CPU负载和各个CPU的使用情况
		1. CPU负载是所有核心的累加结果，每个核满载为1
		2. 单个应用进程如果超过80%，就需要考虑应用本身的逻辑问题；
		3. iowait指标超过5%,或者日志中显示有大量的应用同时发生anr,可以认为是系统引起的卡顿；
	4. event日志：binder_sample：出现Binder通信阻塞时，可以通过这个tag过滤binder的使用情况。
# RxJava
1. Rxjava是通过观察者模式和装饰模式实现一个数据流处理框架
	1. Observable（被观察者）：负责发送数据
	2. Observer（观察者）：订阅Observable 并处理事件；
2. 通过各种操作符支持复杂事件流的组合和变换
	1. 每个操作符都会实现对上游被观察者的装饰，返回一个新的被观察者；
	2. 当订阅发生时，操作符对应的被观察者在对原始的观察者进行一次装饰，交给上游的被观察者；
	3. Map操作符对应的ObservabeMap 和MapObserver
	4. subscribeOn操作符对应ObservableSubscribeOn 和SubscribeOnObserver;
	5. 与标准的装饰者模式相比，数据流的流向是按照操作符的顺序流动，最后才到原始的观察者；
	6. Rxjava 就有一个缺点
		1. subscribeOn直接让上游的被观察者的订阅发生在子线程中，如果下游不通过observerOn切换，观察者也会在子线程中工作。
		2. 数据流的变换操作是通过改变观察者的行为来实现，这与设计理念是不符合的。

# ARouter
1. 利用APT生成路由，定义RouteProcessor注解解释器，按照模块分类，生成不同的填充路由表方法，路由表中路径为key,Activity的class名为value;
2. 各个模块的管理类也由apt生成，为了统一路由的入口，初始化路由表操作提供了2中方式
	1. 编译时字节码插桩，修改入口方法；
	2. 运行时解析dex文件：
		1. 获取所有ARouter相关的类名，根据指定包名前缀，反射初始化，保存类名和版本号保证同一个版本只初始化一次；
	
# 架构设计
## SOLID
1. 单一职责
2. 开闭原则
3. 里氏替换
4. 接口隔离
5. 依赖倒置
## MVx
1. 每个应用至少可以分为2层，UI层也就是View层，负责在屏幕上展示应用数据，数据层也就是Model层，包含应用的业务逻辑并提供应用的数据。额外的还可以添加一层，用来简化和重复使用页面层与数据层之间的交互
2. 在MVC，这一层称为Controller层，通常由Activity和Fragment承担。由于xml的UI层功能较弱，Activity和Fragment还会承担大部分的UI层功能，导致代码臃肿，逻辑复杂；
3. 在MVP中，这一层称为Presenter层，为了隔离UI操作与业务逻辑。将View借口话，Presenter通过实现View接口来更新UI；MVP模式的缺点
	1. 模板代码冗余，每个页面都需要单独的View接口，
	2. Presenter层与View层强耦合，UI交互的修改，需要同步修改View和Presenter
	3. Presenter层持有的View引用，需要手动管理View的生命周期；
4. 在MVVM中，进入ViewModel,不在持有View的引用，提供数据流让View层订阅；
	1. 核心实现依赖2个jetpack组件，Livedata与Viewmodel，它们都能感知生命周期，同时Livedata可以在数据变化是主动通知观察者，借助DataBinding还可以实现双向绑定；
# Livedata/ViewModel/Lifecycle
## Lifecycle
1. 基于观察者模式实现，ComponentActivity会实现LifecyclOwner接口，创建LifecycleRegistry对象，它充当一个集合的作用，保存所有的Lifecycle的观察者；
2. 并向当前页面注入一个空白Fragment或者AndroidR之后直接注册Activity的生命周期监听回调监听Activity的生命周期变化，当生命周期发生变化是，遍历观察者列表，通知观察者；
## Livedata
1. 基于观察者模式实现，将所有的观察者保存集合中；
2. 对于指定了LifecycOwner的观察者 也就是Activity，还会将当前的观察者与Activity的生命周期绑定；只在Resume时接受数据变化，同时Activity销毁时，自动移除观察者；
3. 对于未指定LifecycOwner的观察者，需要手动移除，不然会有内存泄露的风险；
4. Livedata的粘性数据
	1. 2个页面同时使用一个Livedata，启动另一个页面，会接受到Livedata最后一次发送的数据；
	2. 粘性数据产生的原因是因为有2个条件可以触发通知观察者
		1. Livedata主动setValue：观察者保存在Livedata的观察者列表中
		2. Activity启动生命周期变化为Resumed状态；
		3. 数据在下发过程中，首先检查Activity的状态，在检查观察者数据的版本号与LiveData的版本号，
		4. 粘性数据的解决方案，就是通过反射hook Livedata的observe方法，在注册新的观察者时就将观察者的版本号改成和最新Livedata一致
## ViewModel
1. Activity会实现ViewModelStoreOwner接口，创建ViewModelStore对象，通过map来保存所有的ViewModel
2. 当Configuration变化如横竖屏切换时，通过onRetainNonConfigurationInstance ，保存ViewModeStore;
3. Activity重建是，通过getLastNonConfigurationInsance 恢复ViewModelStore;


# 内存泄露
1. 原因：内存泄露的根本原因就是对象的存活时间超出了预期的生命周期；
2. 常见场景
	1. 静态实例持有外部对象致使外部对象无法回收；
		1. 常见的单例中错误持有Activity类型的context
	2. 非静态内部类持有外部类对象，内部类对象的生命周期超过外部类
		1. 在Activity中使用非静态的Handler
			1. 静态内部类+弱引用形式
			2. 在onDestory中通过handler.removeCallbackAndMessages(null)移除回调；
	3. 异步线程/callback持有外部对象
	4. 集合类子项持有外部对象的引用，未及时清除
## Leakcanary的实现原理
Leakcanary实现了对Activity/Fragment/Searvice/View对象的泄露检查，实现原理分为2步
1. 监听这4个对象的创建和销毁
	1. Activity/Fragment
	2. Service
	3. View
2. 通过弱引用和引用队列的形式检查对象是否被成功回收，来判断是否产生泄露；
	1. 当对象被销毁时，加入到弱引用引用队列中，5s后检查这个队列，如果这个对象被成功回收，队列出会出现该


# 插件化
插件化就是动态加载apk文件的过程，一个apk文件包含dex文件/资源文件/so文件/manifest文件，插件的流程也可以分为4步
1. 加载dex文件
	1. dex文件是class文件的集合，加载时同样遵循双亲委托机制;
	2. Android中存在2中ClassLoader,PathClassLoader负载加载应用类,BootClassLoader 加载系统类，同时PathClassLoader的parent属性会指向ClassLoader;
	3. PathClassLoader通过dex文件路径进行加载，加载完成后，将class数据保存在数组中；
	4. 插件化加载dex文件的流程：通过自定义ClassLoader加载插件中dex文件，加载完成后与主APP的PathClassLoader中的dex数组进行合并，合并之后就能通过反射访问；
2. 加载so文件
	Android中存在2中加载so的方式
	1. System.loadLibrary：加载apk中预制的so
	2. System.load:指定so文件的绝对路径进行加载。
	3. 插件化通过第二种方式进行加载
3. 加载资源
	1. 资源ID是一个32位的16进制数
		1. 2个字节的包段：标识资源所属的包，01为系统资源，7f为应用资源
		2. 2个字节的类型段：标识资源类型，如layout,string,drawable;
		3. 4个字节的条目段：标识资源具体的编号
	2. 插件apk与主apk资源如果按照正常方式打包，包段相同，会存在资源冲突，最简单的解决方法就是通过自定义gradle的Transform修改资源ID的包段
	3. 主app的资源进行合并：资源的加载是通过AssetMananger,同样也是通过apk资源文件的路径进行创建；
4. 启动插件Activity：todo?
# 插件化换肤
插件化换肤的流程：
1. hook应用中View的创建过程，保存需要动态替换的View及其对应的属性；
2. 创建插件Resource对象，通过插件中资源文件的路径，加载插件中的资源，保证与宿主中的资源名称和类型一致，通过名称和类型从插件Resource中查找属性；

如何Hook?
1. View的创建过程是通过LayoutInflater解析xml后，利用View的全类名反射创建；
	1. 在反射前，Inflater提供了一个Hook点，允许应用自定反射过程，比如androidx  中的AppCompatActivity，就会通过这个方法将TextView ，ImageView 替换为AppCompatTextView,AppCompatImageView ；
	2. 参考AppCompatActivity这个写法，在Activity onCreate前，替换Inflater的创建方法；
2. 如何修改隐藏属性？


# 其他

## 进程与线程区别？

1. 进程是系统进行资源分配和调度的独立单元，代表程序的一次执行实例，每个进程拥有独立的地址空间和系统资源。
2. 线程是进程内的执行单元，是CPU调度的基本单位，一个进程可包含多个线程，共享进程的资源。

## 进程间的通信方法

除了传统的 文件共享，Socket ，共享内存。还有基于Android Binder 衍生的AIDL、Messenger 、ContentProvider.

## 为什么匿名内部类访问的局部变量要用final修饰
确保局部变量的生命周期与匿名内部类对象解耦，避免数据不一致和内存访问错误；
1.  局部变量的生命周期是有可能比匿名内部类对象的生命周期短；
2. 为了保证局部变量被销毁，内部类对象依然可以访问， 匿名内部类会被编译为一个独立的类文件，局部变量会复制一份成为匿名匿名内部类的成员变量；
3. 为了保证局部变量和其副本的值一致，采用final修饰；
## OutOfMemoryError 是否可以捕获
从语法层面上可以捕获，但捕获之后，不能保证程序能正常运行，依然可能因为内存不足继续奔溃，对一些可控的场景进行捕获
1. 图片加载失败是，降级使用低分辨率图；
2. 释放一些非关键内存；
## Class.forName 与ClassLoader.loadClass的区别
1. Class.forName 默认会触发类的初始化，执行静态代码块，初始化静态变量；
2. ClassLoader.loadClass 只会触发加载和连接过程；
3. ClassLoader会使用当前的类的加载器，ClassLoader需要指定加载器
## 如何加载大图
1. 采样压缩
	1. 根据图片真实大小与实际需要大小计算采样率
	2. 选择合适的图片格式，RGB565/RGB888,前者一个像素占用2个字节，后者占用4个字节
2. 对于超大图片采用BitmapRegionDecoder分块加载
	1. 按照需求指定加载固定大小的图片；
## Bitmap的内存计算方式
1. 内存大小= 实际宽度x实际高度x每个像素的字节数
2. 对于资源目录的图片还会考虑缩放比例：缩放比例 = 设备dpi/资源目录dpi
3. 设备dpi为每英寸上的像素数，基准dpi为160，px = dp*(设备DPI/基准DPI)
	1. xxdpi:480，xdpi:320