# 自我介绍

你好，我叫李乔，2014 年毕业于湖北大学，至今已有 12 年的 Android 开发经验，从事过智能硬件+互联网经融+系统应用的开发。目前在追觅负责 系统桌面的基础体验功能实现，主要包含桌面的拖拽布局和动画优化。上一份工作是在 realme 公司负责系统桌面的商业化变现，为桌面添加了抽屉智能搜索，文件夹商业化平台SDK的插件化以及文件夹底部滚动广告和桌面底部浏览器搜索框等一系列的商业化功能，也积累了一定的内存优化，卡顿优化的实战经验。在之前的工作中，还主导过动态化换肤方案和应用的冷启动速度优化方案落地，以上就是我的自我介绍，谢谢

# 桌面拖拽流程
以`1*1`的图标为例，介绍图标的拖拽流程。
1. 长按图标会触发预拖拽，生成DragView，对应用图标来说就是只包含图片不包含标题的ImageView;
2. 同时创建对应的DragDriver，桌面处理触摸事件是通过DragLayer下发，当DragLayer接收到触摸事件后，遍历TouchController列表进行分发，只有DragDriver不为空时，才下发到DragController，后续的触摸逻辑由DragController接管；
3. 当手指移动超过阈值时，结束预拖拽，隐藏对应的BubbleTextView，同时桌面进入到Spring_Loaded状态，执行对应的状态动画；
4. 手指移动过程中，根据每个网格矩形的中心点坐标距离DragView的中心距离，找到最近网格
	1. 如果网格中已经存在图标则根据停留时间与距离触发组成文件夹和挤位逻辑，不存在图标，直接绘制落点预览背景；
5. 松手后，移除原始BubbleTextView，并加入到目标网格对应的CellLayout中，修改数据库中布局数据，播放位移动画将DragView移动到目标网格，动画结束后，显示出BubbleTextView；
其中最复杂的就是挤位坐标的计算
6. 第一步就是根据DragView与重叠的目标网格的位置计算出挤位方向，这是一个大小为2的向量数组，由它们与坐标轴的角度计算得到，值可以为 -1,0,1。组合成上下左右和4个对角线8个方向；
7. 第二步根据重叠网格内的现有图标，构造出目标区域矩形，对于应用图标挤压应用图标的场景矩形大小就是`1*1` ，其他如widget，根据widget大小进行计算；
8. 第三沿着挤位方向，整体推移图标，对于上下左右方向，则修改x,y，如方向向右，则所有的x+1。对于对角线方向，进行坐标拆分会先尝试左右,之后在上下，推挤过程会传递，如果推A的过程遇到B，B也会被一起推；
9. 原生逻辑在挤位过程中，超过边界会挤位失败，失败后反转向量数组，尝试向反方向进行推移，对角线方向就会尝试4次；
10. 挤位失败则尝试将重叠图标保持相对位置不变整体移动到最近的空位上。
11. 移动失败则单独为每个重叠的view找空格图标；
12. 得到全新的坐标后，播放位移动画；
## Dock拖拽的自动补位
1. 原生：拖拽出一个图标后，会保留空位
2. 场景有3种：
	1. Dock拖出到桌面：这种最简单，参考荣耀的实现，只需要在松手后，也就是onDrop方法中，遍历Dock中的图标，按原始顺序向前挤位， 重新计算位置后 播放动画；
	2. 桌面拖入Dock：分为挤位和组成文件夹，对于挤位场景，需要设置临时变量，将当前Dock的图标的数量+1，用于重新计算每个图标的新位置播放动画；
	3. Dock内拖动挤压和组成文件夹，需要在查找最近网格时，保证挤位不会超出Dock限制即可；

# 一帧数据的绘制流程
1. Choreographer创建时，会同步初始化FrameDisplayEventReceiver与主线程Handler以及CallbackQueue；
2. 其中FrameDisplayEventReceiver用于与SurfaceFlinger建立通信，用来接收和请求Vsync。
3. 当V-sync信号到达后,回调onVsync,同时向主线程发布同步屏障消息和post自己，它的run方法中会调用doFrame；
4. doFrame中就会依次执行CallbackQueue的input/animation/insets animation /traversal 回调
5. input回调由ViewRootImpl分发事件时添加；
6. animation回调常见的startScroll 和 属性动画开始时会添加
7. traversal回调如invalidate和requestLayout都会添加
8. traversal回调会进入到ViewRootImpl.performTraversals方法，依次执行view的测量，布局和绘制；
9. 绘制完成后，每一帧的 doFrame 的时候，都会根据情况触发下一个 Vsync 的申请
## 桌面的启动动画
1. Launcher启动应用时会通过ActivityOptions 包装RemoteAnimation，作为参数传递给WMCore，当应用窗口绘制完成，Core回调WMShell的onTransitionReady启动动画；
2. 之后开始播放组合动画，包括 应用图标+窗口动画以及桌面的内容动画和壁纸的模糊动画；
	1. 应用的图标动画通过创建`FloatingIconView,生成一个浮动视图,让图标在 DragLayer 上"脱离"原位置。
	2. 应用窗口动画通过`SurfaceTransactionApplier` 和 `Matrix` 去操控 appTargets的leash`(SurfaceControl),让应用窗口从图标尺寸展开到全屏尺寸。
	3. 桌面的内容如Workspace Hotseat PageIndicator 做淡出 + 轻微缩放；
	4. 壁纸的模糊动画：`BlurOverlayAnim` 同步模糊/壁纸深度。
3. 其中FloatIconView与窗口动画的做联动动画，它们的alpha互斥：图标逐渐淡出，窗口逐渐显影
	1. 设置最大缩放比例使图标放大到能铺满窗口最短边；
	2. 对于窗口则 按照一定放大比率裁剪显示区域，默认从中心开始放大。裁剪完之后，根据它与图标的大小比率再缩放到图标大小，并平移到图标所在位置
4.  帧同步的实现：通过给 FloatingIconView 的 `ViewRootImpl`,注册`registerRtFrameCallback 回调，在回调中调用 mergeWithNextTransaction`,让 Launcher 的 RenderThread 帧与 SurfaceControl 事务合并到同一次 SurfaceFlinger 提交。
# 抽屉卡顿
1. trace上显示在关闭新机保护展示出推荐应用的广告数据之后，在滑动时，onLayout 异常调用多次，导致抽屉滑动卡顿；
2. 问题产生的背景：
	1. 抽屉列表是RecyclerView+ 字母滚动条的组合,用于展示应用数据，当抽屉滚动时，根据屏幕首个显示item的position和对应viewtype 计算滚动距离，从而得到滚动条需要滚动的百分比距离；
	2. 对于原生抽屉来说，抽屉中的View 都是BubbleTextView,高度是固定的，计算时遍历item 根据itemtype类型可以直接获取到高度。而新加入的广告类型高度没有缓存，需要在滚动时重复测量；
	3. 广告数据越多卡顿越明显，国内的测试得不到足够多的广告，由于新机保护7天不显示广告，前端测试漏了这个问题；
3. 解决方案：
	1. 缓存对应itemViewtype的高度；
	2. 优化了DiffUtils，只保留应用标题和图标判断；
	3. 减少onCreateViewHolder和onBindViewHolder对象的创建；
		1. 如ClickListener的创建和AppInfo数据的状态检查；

# Shell Transition
以 Launcher 冷启动应用为例介绍 Shell Transition 的流程
1. Shell Transition 分为 2 个模块，WMCore/WMShell
	1. WMCore 是运行在 system_server 进程的模块，核心类包含 
		1. TransitionController： 主要负责管理者整个过渡动画的生命周期，比如动画参与者Wincontainer 的收集，等待，启动等;
		2. Transition：具体过渡动画的实体类，它的主要生命周期包含收集中（collectiing），启动（started） ，播放中（playing），结束（finished)；
	2. WMShell 运行在 SystemUI进程的模块，核心类 包含
		1. Transitions：主要负责相关过渡动画的具体播放相关逻辑
		2. ActiveTransition：具体过渡动画的实体类
2. 启动应用时，Launcher进程会构造ActivityOptions将RemoteTransition打包，RemoteTransition会实现startAnimation方法接受leash用于同步播放窗口动画；
3.  在system_server 进程中 ActivityStarter启动对应Activity前，通过TransitionController创建Transition，将状态置为收集中，同时初始化SyncGroup；
	- 对于启动动画来说，会收集4个WindowContainer，加入到SyncGroup，也就是应用的ActivityRecord和Task，Launcher的ActivityRecord和壁纸
	- 同时会将StartingWindow 挂载到ActivityRecord下，跟随 ActivityRecord 的Surface 一起动画；
4. 之后通知Shell，完成ActiveTransition和TransitionHandler的初始化，同时Transition进入启动状态；
5. 当StartingWindow完成绘制之后，通知 WMS的BlastSyncEngine，判断SyncGroup中所有窗口的Surface已经完成绘制，回调core的onTransactionReady和shell的onTransitionReady，进入播放状态；
6. 获取ActivityOptions的RemoteTransition进行动画的播放，动画结束，回调finishCallback;
7. 在Shell Transition中，Core 收集窗口，Shell 负责播放窗口动画，动画播放前，core 会准备startTransaction 和 finishTransaction 
	1. startTransaction：找到窗口的公共祖先，新建根节点，将所以窗口转移到新节点，统一播放；
	2. finishTransaction ，动画结束后回调，将节点reparent再到正常窗口树；
	3. 实现所有参与窗口动画的统一播放，播放结束后 ，有些窗口要隐藏，有些要显示；
	