
# Compose Navigation
 1. NavController：导航控制器，负责管理应用的导航状态，跟踪当前显示的目的地，处理导航动作
2. NavGraph：导航图，定义所有目的地及其之间的连接关系，是导航的蓝图
3. Destination：目的地，代表应用中的一个屏幕（Composable 函数）
4. Action：导航动作，定义从一个目的地到另一个目的地的跳转
5. NavBackStackEntry存储了导航中回退栈的信息。可以通过以下方式获取

``` kotlin
val backstackEntry = navController.currentBackStackEntryAsState() 
val route = backstackEntry.value?.destination?.route //获取当前的路
```

# CompositionLocalProvider
1. CompositionLocal是中用于组件树内隐式数据传递的核心机制，其设计初衷是解决跨多层组件的数据共享问题，避免通过函数参数逐层传递数据。
	1. 设置一个默认值，使用中通过中缀函数修改，其他组件读取到修改值
2. `compositionLocalOf` 当提供的值改变时，只有直接读取该值的组合项会重组
3. `staticCompositionLocalOf`当提供的值改变时，会导致整个 `CompositionLocalProvider` 的 `content` lambda 重组所有引用该值的地方
# Modifier
1. 修饰符顺序影响结果：Modifier 的调用顺序会影响最终布局，例如 `padding` 与 `clickable` 的顺序会影响点击范围；
## Modifier.graphicsLayer
## Modifier.pointerInput
1. Modifier.pointInput()方法最前面的参数是Key，用于区分当前手势的一个标志位，如果在手势未停止的过程中，key值改变了，那么事件的处理模块会立即取消并重新启动。
2. detectDragGestures()：监听手指的拖动事件，并且可以通过dragAmount参数获取水平和垂直方向的偏移量；
3. detectTransformGestures：双指变换手势
	1. - centroid：中心位置的偏移量，为Offset类型；
	2. pan：这个参数乍一看名字不太理解是干啥的，它其实是水平和垂直方向上的偏移量，也是一个Offset类型，可以用参数做组合项移动的事件；
	3. zoom：缩放参数，为Float类型，它是以1F为基数，做缩放处理的时候需要累乘；
	4. rotation：旋转角度参数，为Float参数，顺时针方向为正数，逆时针方向为负数，做旋转处理的时候需要累加。
4. 双指缩放和旋转除了使用PointInput-detectTransformGestures方式之外，还可以直接使用Modifier.transformable()方式实现
## 宽高
|       |                                                     |                                                                                                                                                                                       |
| ----- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 固定尺寸  | `Modifier.size(100.dp)`                             | 设置固定的宽度和高度 [](https://juejin.cn/post/7000153533866016775)[](https://blog.csdn.net/weixin_42473228/article/details/142284322)                                                          |
|       | `Modifier.width(100.dp)` / `.height(50.dp)`         | 单独设置宽度或高度 [](https://juejin.cn/post/7000153533866016775)[](https://blog.csdn.net/weixin_42473228/article/details/142284322)                                                           |
| 充满父布局 | `Modifier.fillMaxWidth()` / `.fillMaxHeight(0.5f)`  | 填充父布局的整个或部分（如50%）可用空间 [](https://juejin.cn/post/7000153533866016775)[](https://blog.csdn.net/weixin_42473228/article/details/142284322)[](https://juejin.cn/post/7520175773220782107) |
| 按比例缩放 | `Modifier.aspectRatio(16f/9f)`                      | 设置宽高比（如16:9），通常结合其他尺寸修饰符使用 [](https://blog.csdn.net/weixin_42473228/article/details/142284410)                                                                                        |
| 尺寸约束  | `Modifier.sizeIn(minWidth=100.dp, maxWidth=200.dp)` | 将尺寸限制在最小和最大值之间 [](https://juejin.cn/post/7000153533866016775)                                                                                                                         |
|       | `Modifier.wrapContentSize()`                        | 让控件尺寸由其内容决定 [](https://blog.csdn.net/weixin_42473228/article/details/142284322)                                                                                                       |
|       | `Modifier.requiredSize(500.dp)`                     | 强制设置尺寸，即使超出父布局约束                                                                                                                                                                      |
## Modifier .offset
1. 指定水平和垂直方向上的偏移量；
## Modifier.onGloballyPositioned
这个修饰符会在可组合项完成布局后调用，并通过 `LayoutCoordinates` 提供其尺寸和位置信息。
# 布局
## Row

1. Row ：X轴 、
2. horizontalArrangement:子元素的水平方向排列效果
	1. Arrangement.SpaceEvenly:左右对齐排列,且各组件元素间距相同
## Column
1. Column：Y轴、
## Box
 1. Box：Z轴
## Image
1. contentScale
	1. ContentScale.Crop
2. painter
	1. painterResource
## BoxWithConstraints
# 动画
# Animatable
1. snapTo:**立即**将当前值切换到目标值,不会产生动画，立即打断动画；
2. `animateTo`：从当前值**平滑过渡**到目标值
## AnimatedVisibility

# 其他
## BackHandler
当用户按下设备的返回按钮时，阻止默认的返回行为，执行自定义逻辑。

# Todo

1. Scaffold:标准化的页面骨架,将常见的页面元素以槽位的显示预先定义好位置
2. shadowElevation = 0.dp,
	1. tonalElevation =  0.dp
3. sealed class
4. data class 的 copy
5. fillMaxSize
6. kotlin infix


# 参考链接
- [# Jetpack篇之 Compose Navigation 全指南](https://blog.csdn.net/weixin_37794278/article/details/150482560) 
- [# Compose的Navigation(十五)](https://blog.csdn.net/Mr_Tony/article/details/118971169)
- [# 【Jetpack Compose】自定义手势事件之PointerInput](https://article.juejin.cn/post/7289718324858028089)